package isa.project.flight.dto;

public enum Sort {
	cheapest, highest_price, quickest, slowest, 
	earliest_takeoff1, earliest_takeoff2, latest_takeoff1, latest_takeoff2,
	earliest_landing1, earliest_landing2, latest_landing1, latest_landing2
}